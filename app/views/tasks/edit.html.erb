<h1>タスクの編集</h1>

<%= form_with(model: @task, local: true) do |f| %>
    <%= f.label 'タスク' %>
    <%= f.text_field :content %>
    <%= f.submit '修正' %>
<% end %>

<%= link_to 'タスク一覧へ戻る', tasks_path %>
    